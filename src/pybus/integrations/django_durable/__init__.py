"""Opt-in Django persistence for durable Pybus commands.

Import the store from ``pybus.integrations.django_durable.store``. Keeping the
app package import side-effect free lets Django populate its app registry.
"""
